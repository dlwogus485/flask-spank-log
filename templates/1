<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>제출 이력</title>
</head>
<body>
  <h2>내 제출 이력</h2>
  {% if lines %}
    <ul>
    {% for line in lines %}
      <li>{{ line }}</li>
    {% endfor %}
    </ul>
  {% else %}
    <p>제출한 보고서가 없습니다.</p>
  {% endif %}

  <br>
  <a href="/">홈으로</a><br>
  <a href="/logout">로그아웃</a>
</body>
</html>

